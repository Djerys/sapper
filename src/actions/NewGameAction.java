package actions;

import actions.base.Action;
import ui.GameClient;

public class NewGameAction extends Action {
    public NewGameAction(GameClient client) {
        super(client);
    }

    @Override
    public void process() {
    }
}
